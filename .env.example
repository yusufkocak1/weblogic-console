# Port the local console listens on
WLC_PORT=7101

# Bind address. Loopback by default — see the security notes in README.md
# before changing this.
WLC_HOST=127.0.0.1

# Development only: where Vite proxies /api during `npm run dev`.
VITE_BACKEND_URL=http://127.0.0.1:7101
